/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.clerk.com', 'randomuser.me', 'images.unsplash.com', 'res.cloudinary.com']
    },
};

export default nextConfig;
